export enum EventCategory {
  DEFAULT = 'default',
  MEET = 'meet',
  HOLIDAY = 'holiday',
}

export enum EventStatus {
  CONFIRMED = 'confirmed',
  TENTATIVE = 'tentative',
  CANCELLED = 'cancelled',
}
